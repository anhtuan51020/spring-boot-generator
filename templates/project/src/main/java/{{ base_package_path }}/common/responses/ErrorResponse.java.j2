package {{ base_package }}.common.response;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.time.LocalDateTime;
import java.util.Map;

/**
 * Wrapper cho lỗi trả về cho client.
 *

 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Schema(description = "Thông tin lỗi chuẩn hóa cho mọi response lỗi")
public class ErrorResponse {

    @Schema(description = "Mã lỗi ngắn (ERR-...)")
    private String errorCode;

    @Schema(description = "traceId để truy dấu")
    private String traceId;

    @Schema(description = "Error message")
    private String message;

    @Schema(description = "Error details")
    private Map<String, String> details;

    @Schema(description = "Timestamp")
    private LocalDateTime timestamp;
} 